package main

// NickList holds a list of nicks always in most recently used order
type NickList struct {
	list []string
}

func (nicklist *NickList) pushNick(nick string) {
	//delete the nick if it exists
	//push the nick to the head of the list

}
